# #!/bin/bash
# sudo yum install unzip wget httpd.x86_64 -y
# sudo wget https://github.com/utrains/static-resume/archive/refs/heads/main.zip
# sudo unzip main.zip -d /var/www/html
# sudo cp -r /var/www/html/static-resume-main/iPortfolio/* /var/www/html/
# sudo systemctl start httpd.service
# sudo systemctl enable httpd.service